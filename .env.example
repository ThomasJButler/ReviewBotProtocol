# Where the ReviewBot Protocol backend is listening. Server-side only.
BACKEND_URL=http://127.0.0.1:8000
# Must match LOCAL_API_TOKEN in backend/.env. Never sent to the browser.
LOCAL_API_TOKEN=
# Next telemetry is switched off by the npm scripts (NEXT_TELEMETRY_DISABLED=1).

[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -ra --tb=short --strict-markers
markers =
    e2e: needs a running Ollama and is skipped unless REVIEWBOT_E2E=1
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
